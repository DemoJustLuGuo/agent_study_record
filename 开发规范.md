# AI Agent 框架 开发规范（团队内开发版）

> 版本：2026-04-08（基于当前仓库代码快照）

## 1. 文档目标

本开发规范面向团队内部开发，聚焦三件事：

1. 当前架构长什么样（模块边界与运行链路）
2. 目前开发不足在哪里（已识别技术债）
3. 下一步可扩展什么（可落地的演进方向）

---

## 2. 当前架构

### 2.1 总体架构图（文件树视角）
```text
agent_study_record/
|-- app/
|   |-- main.py                       # 启动入口（仅 launch）
|   |-- app_builder.py                # Gradio 页面组装入口
|   |-- runtime.py                    # UI 回调与服务调用逻辑
|   |-- ui/                           # 分页 UI 模块（chat/rag/knowledge/connection）
|   `-- ui.css                        # Gradio 前端样式
|-- agent/
|   |-- react_agent.py                # create_agent 编排、路由与流式输出
|   |-- middleware.py                 # monitor_tool/log_before_model/log_after_model/report_prompt_switch
|   `-- tools/
|       |-- registry.py               # 统一工具注册（register_tools/get_registered_tool_map）
|       |-- agent_tools.py            # 兼容导出层（保留历史导入路径）
|       `-- modules/                  # 工具实现拆分目录（rag/web/compute/external/memory/context）
|-- rag/
|   |-- rag_service.py                # RAG 检索+生成（answer_with_references）
|   |-- vector_store.py               # 文档切分、向量入库、manifest/snapshot/rollback
|   |-- knowledge_base.py             # 知识上传与生命周期接口封装
|   `-- memory_service.py             # 长期记忆写入/检索（复用 Chroma）
|-- model/
|   `-- factory.py                    # chat model 与 embedding model 工厂
|-- config/
|   |-- rag.yml                       # 外部业务数据配置
|   |-- chroma.yml                    # 向量检索参数
|   |-- prompts.yml                   # 提示词路径映射
|   `-- agent.yml                     # 模型与推理配置
|-- prompts/
|   |-- main_prompt.txt               # 常规模式提示词
|   |-- report_prompt.txt             # 报告模式提示词
|   `-- rag_summarize.txt             # RAG 摘要提示词
|-- data/                             # 知识源文件
|-- chroma_db/                        # Chroma 持久化目录
`-- logs/                             # 运行与追踪日志
```

主调用链（按文件）：

1. app/ui/chat_tab.py -> app/runtime.py::stream_agent_reply -> ReactAgent.execute_stream
2. agent/react_agent.py -> create_agent(..., tools=register_tools(), middleware=[...])
3. Agent 工具调用 -> agent/middleware.py（monitor_tool）-> agent/tools/registry.py -> agent/tools/modules/*.py
4. 工具内部按需调用 rag/rag_service.py 或 rag/memory_service.py -> rag/vector_store.py -> chroma_db/
5. Gradio “在线RAG/知识库管理”标签页 -> app/runtime.py 事件处理函数 -> rag/knowledge_base.py / rag/rag_service.py

### 2.2 模块职责（按目录）

- app/main.py
	- 程序启动入口，仅负责读取 `APP_HOST/APP_PORT/APP_DEBUG` 并启动 Gradio
- app/app_builder.py
	- 组装全量 Gradio 页面（Header/连接设置/各 Tab/Footer）
- app/runtime.py
	- UI 回调函数与服务层调用（chat/rag/knowledge）
- app/ui/*
	- 按页签拆分 UI 结构：`chat_tab.py`、`rag_tab.py`、`knowledge_tab.py`、`connection_panel.py`
- agent/react_agent.py
  - 封装 create_agent
  - 注入工具列表、系统提示词、中间件
  - 解析流式 tool_call_chunks/tool_calls，并兼容 Minimax `<invoke ...>` 内联调用兜底
- agent/middleware.py
  - 工具调用监控（start/end/error）与 trace 落盘
  - 模型调用前/中/后日志与耗时统计
  - 按 context.report 动态切换提示词
  - 通过 trace_id 维护工具事件队列，供前端思考态展示“正在调用工具：xxx”
- agent/tools/registry.py
	- 统一工具注册与名称映射（register_tools/get_registered_tool_map）
- agent/tools/modules/*
	- 按职责拆分工具实现（RAG、联网搜索、Python/MATLAB 执行、外部数据、长期记忆、报告上下文触发）
- agent/tools/agent_tools.py
	- 兼容导出层，保留历史导入路径以降低迁移风险
- rag/vector_store.py
  - 文档加载与切分
  - Chroma 向量入库与 retriever 构建
  - 生命周期治理：source->md5 索引清单、失效源文件清理、快照与回滚
- rag/knowledge_base.py
	- 文本知识上传服务（upload_by_str）
	- 网页链接抓取入库服务（upsert_web_urls）
	- 生命周期服务透传（sync_removed_sources / create_snapshot / rollback_snapshot）
	- 复用 VectorStoreService，统一向量库与去重逻辑
- rag/config_data.py
	- 兼容层配置映射
	- 将新增模块参数映射到现有 config/*.yml
- rag/rag_service.py
  - RAG 召回与摘要生成链（Hybrid 召回 + 启发式重排）
  - PromptTemplate + ChatModel + OutputParser
- rag/metrics.py
  - 在线 RAG 运行指标聚合（窗口统计、刷新、重置）
- model/factory.py
  - 聊天模型与 embedding 模型工厂
- utils/*
  - 配置读取、路径处理、日志、文件加载、提示词加载

### 2.3 请求执行链路

1. 用户在 UI 输入问题
2. Gradio ChatInterface 调用 `stream_agent_reply`
3. Agent 模式：`ReactAgent.execute_stream` 发起 `agent.stream(..., stream_mode="messages")`
4. 模型触发 function calling 时，经 `monitor_tool` 进入工具执行；工具名与状态被写入 trace 和内存事件队列
5. `execute_stream` 拉取工具事件队列，将“正在调用工具：xxx / 工具执行完成”流式输出到前端思考区
6. 若触发 `fill_context_for_report`，中间件写入 `context.report=True`，后续模型调用由 `report_prompt_switch` 切换到报告提示词
7. 在线RAG模式：`rag_query` 直接走向量检索问答链，返回 answer + references

### 2.4 数据与配置流

- 模型配置：model/config/agent.yml
- 向量库配置：rag/config/chroma.yml
- 提示词路径配置：agent/config/prompts.yml
- 外部业务数据配置：agent/tools/config/rag.yml
- 知识数据目录：data/
- 向量持久化目录：chroma_db/
- 文件去重指纹：md5.text
- 生命周期清单：chroma_manifest.json
- 生命周期快照目录：chroma_snapshots/

### 2.5 Middleware 模块构成与作用

`agent/middleware.py` 作为 Agent 的运行时控制面，核心由 5 类组件构成：

1. 追踪与日志基础函数
	- `_trace_id/_log_prefix/_write_trace`：统一 trace_id 与 JSONL 追踪输出（`logs/traces/<trace_id>.jsonl`）
	- `_preview_text/_preview_message/_message_snapshot`：对参数、消息与结果做截断预览，避免日志过大

2. 工具调用中间件（Function Calling 观测点）
	- `@wrap_tool_call monitor_tool`
	- 负责记录工具调用开始/结束/异常，统计耗时，写入 `tool_start/tool_end/tool_error`
	- 在调用 `fill_context_for_report` 时设置 `runtime.context["report"] = True`

3. 模型调用中间件
	- `@before_model log_before_model`：记录调用前上下文与消息快照
	- `@wrap_model_call log_model_call`：统计模型调用耗时并记录输入输出摘要
	- `@after_model log_after_model`：记录最终输出消息概览

4. 动态提示词中间件
	- `@dynamic_prompt report_prompt_switch`
	- 依据 `context.report` 在 `main_prompt` 与 `report_prompt` 间动态切换

5. 前端思考态事件队列
	- `_push_tool_event/pop_tool_events/clear_tool_events` 按 trace_id 维护工具事件
	- `ReactAgent.execute_stream` 拉取该队列，将“正在调用工具：xxx”等状态流式显示到前端

---

## 3. 本地开发与调试流程

### 3.1 建议启动顺序

建议优先安装完整依赖：

```bash
pip install -r requirements.txt
```

若启用 `matlab` 工具（当前实现基于 `matlab.engine`），需额外安装 MATLAB Engine for Python：

```bash
# 1) 进入 MATLAB 安装目录下的 Python engine 源码目录
cd <MATLABROOT>/extern/engines/python

# 2) 在当前项目使用的 Python 环境中安装
python -m pip install .
```

安装后建议先做环境检查：

```bash
# 检查 langchain-experimental 与 python REPL 工具是否可导入
python -c "from langchain_experimental.tools.python.tool import PythonREPLTool; print('PythonREPLTool OK')"

# 检查 matlabengine 是否可导入
python -c "import matlab.engine; print('matlabengine OK')"

# 检查 MATLAB Engine 是否可启动（成功后会输出 MATLAB 版本）
python -c "import matlab.engine; eng = matlab.engine.start_matlab(); print(eng.eval('version', nargout=1)); eng.quit()"
```

```bash
python rag/vector_store.py
python -m app.main
```

Windows 可使用以下启动脚本：

```bat
launch.bat
```

```powershell
.\Launch.ps1
```

日志等级可通过环境变量 `LOG_LEVEL` 控制（低于该等级的日志不会输出）：

```powershell
$env:LOG_LEVEL = "INFO"
```

说明：默认监听 `APP_HOST/APP_PORT`（缺省 `127.0.0.1:7860`），浏览器访问后使用 Gradio 页面交互。

### 3.2 开发常用检查点

- 检查配置是否可读：model/config/*.yml、rag/config/*.yml、agent/config/*.yml 路径和键名
- 检查提示词是否有效：agent/prompts/*.txt、rag/prompts/*.txt 当前应为可执行业务提示，而非占位文本
- 检查计算工具依赖：`langchain_experimental` 与 `matlab.engine` 可正常导入
- 检查向量库是否已有数据：chroma_db/ 与 md5.text
- 检查日志输出：logs/

### 3.3 知识库上传与网页抓取（Gradio）

- Gradio 页面“知识库管理”页签包含文本上传区与网页链接抓取区
- 事件处理函数：`upload_knowledge`、`ingest_web_urls`
- 上传/抓取逻辑会做内容去重，重复内容会返回跳过提示

### 3.4 在线RAG问答（Gradio）

- Gradio 页面“在线 RAG”页签直接调用 `rag_query`
- 返回 answer 和 references（命中段落），页面展示答案与命中片段摘要
- 页面提供在线指标面板：`rag_metrics_refresh` / `rag_metrics_reset`

### 3.5 向量生命周期管理（CLI + Gradio）

- 命令行方式：
	- 全量增量同步：`python rag/vector_store.py load`
	- 删除失效源文件向量：`python rag/vector_store.py sync`
	- 创建快照：`python rag/vector_store.py snapshot --tag release_note`
	- 回滚快照：`python rag/vector_store.py rollback 快照目录名`
- Gradio 页面“知识库管理”页签已提供以上三类操作入口（同步、创建快照、回滚快照）。

---

## 4. 当前开发不足（技术债清单）

以下问题按“优先级 + 影响面”整理，供迭代排期。

### 4.1 高优先级（新增，建议优先修复）

1. 缺少安全边界：高风险工具与管理接口默认开放
	- `python` / `matlab` 工具允许执行任意代码，当前未做沙箱隔离、命令审计白名单、资源限额分级。
	- 知识库管理操作（sync/snapshot/rollback）当前通过 Gradio 事件直接触发，尚未引入独立鉴权与角色分级控制。
	- 影响：存在被提示词注入或恶意请求放大后的高危操作风险。

2. 向量生命周期在“同内容多来源”场景存在一致性缺陷
	- 文件扫描中，当新文件 MD5 已存在时会仅补 manifest 映射而不补向量；后续若原始来源文件删除，会按 source 删除唯一向量。
	- 删除后 manifest 可能仍保留新来源映射，但向量已不存在，造成“清单存在、检索缺失”的隐性数据丢失。
	- 影响：知识库召回结果可能悄然退化，且难以第一时间被业务层感知。

3. 错误信息与内部路径暴露
	- API 失败时直接向前端返回 `str(exc)`，可能泄露内部实现细节。
	- 检索 references 透传 metadata，`source` 当前为绝对路径，前端可直接显示服务器文件系统路径。
	- 影响：提升攻击面并暴露运行环境信息，不符合生产安全基线。

### 4.2 中优先级（新增，可并行修复）

1. 提示词授权名与实际工具名不一致
	- 提示词中使用 `external_api`，实际工具注册名为 `fetch_external_data`。
	- 影响：模型工具选择准确性下降，可能导致“应调用未调用”或冗余重试。

2. 依赖与文档存在漂移
	- 开发规范历史内容仍包含 Flask `/api/*` 与 Electron IPC 链路描述，和当前 Gradio 主入口存在偏差。
	- 启动脚本命名已统一为 `launch.bat` 与 `Launch.ps1`，后续新增文档需保持一致。
	- 影响：团队认知不一致，增加环境安装与排障成本。

3. 配置与异常处理鲁棒性不足
	- 配置读取未建立 schema 校验，错误键名或类型在运行期才暴露。
	- 多处保留 `raise e` 重新抛出写法，不利于保留原始栈信息。
	- 影响：故障定位效率下降，启动期缺少前置失败保护。

4. 上传链路缺少体积限制
	- 文本上传接口未显式配置最大请求体，超大文件可能拉高内存占用。
	- 影响：在并发场景下容易触发资源抖动。

### 4.3 低优先级（架构演进期处理）

1. 缺少自动化测试
	- 工具层、RAG 层、中间件层、生命周期链路尚未建立单元与回归测试。

2. 可观测性仍偏弱
	- 目前以文本日志为主，缺少统一 trace_id、链路级耗时与失败维度统计。

3. 工具层存在可维护性冗余
	- 存在未使用的全局变量（如 user_id、month），易引入误导与后续维护噪声。

### 4.4 历史已完成问题（归档）

1. [已完成] 提示词占位问题
2. [已完成] 工具返回契约不一致（fetch_external_data 返回字符串）
3. [已完成] 内部函数误注册（generate_external_data 已移出 tools）
4. [已完成] 时间上下文硬编码（get_current_month 动态化）
5. [已完成] RAG 调试输出污染（print_prompt 移除）
6. [已完成] 类型标注与实际不一致
7. [已完成] 文件扫描异常返回值语义修复
8. [已完成] 依赖锁定文件缺失（requirements.txt 已补）
9. [已完成] 向量生命周期治理能力缺失（manifest/sync/snapshot/rollback 已补）
10. [已完成] 缺少有效忽略规则文件（.gitignore 已补）

---

## 5. 可扩展内容（建议路线）

### 5.1 Agent 与工具层扩展

1. 落地工具安全分层
	- 将工具分级为 safe/read-only/high-risk（如 python、matlab）。
	- 对 high-risk 工具增加显式开关、调用审计、资源配额与超时策略。

2. 建立统一工具契约
	- 提示词授权名、工具注册名、前端说明文案统一到同一清单。
	- 统一入参与返回 Schema（建议 pydantic），降低隐式字符串协议风险。

3. 引入失败兜底策略
	- 工具超时/异常时，返回可解释降级消息。
	- 建立“工具失败 -> 无工具回答”的明确降级模板。

### 5.2 RAG 能力扩展

1. 检索优化
	- 多路召回（关键词 + 向量）
	- 加入 reranker 提升相关性

2. 数据治理
	- 文档 metadata 标准化（source、version、timestamp），避免对前端暴露绝对路径。
	- 重构 MD5 与 source 映射模型（一对多关系），消除同内容多来源场景的数据一致性缺陷。

3. 质量评估
	- 建立离线评测集，度量召回率/回答准确率

### 5.3 提示词与上下文扩展

1. 提示词版本化
	- 按场景维护 prompt v1/v2，并记录变更说明

2. 场景状态机扩展
	- 在 report 之外增加 review、analysis 等模式
	- 用显式状态迁移替代隐式上下文开关

### 5.4 工程化扩展

1. 依赖与环境标准化
	- 按运行场景拆分依赖（runtime/dev/test），清理已下线依赖。
	- 固定关键版本，保证团队复现。

2. 测试与 CI
	- 为工具层、RAG 层、中间件层补单元测试。
	- 为向量生命周期增加回归测试（同 MD5 多来源、删除/回滚场景）。
	- 加入 lint/type-check/test 的最小 CI 流水线。

3. 可观测性
	- 接入 LangSmith 或 OpenTelemetry。
	- 统一链路追踪、错误分级与脱敏策略。

4. 接口安全治理
	- 为知识库管理操作入口增加鉴权（如 Admin Token/JWT + RBAC）。
	- 增加上传体积限制、请求频控与基础审计日志。

---

## 6. 建议迭代节奏（内部参考）

### Sprint 1（安全与一致性）

1. 为知识库管理操作入口加鉴权与审计（sync/snapshot/rollback）。
2. 为 python/matlab 工具增加高风险开关、资源配额与调用审计。
3. 修复同 MD5 多来源的数据一致性缺陷，并补回归测试。
4. 统一错误返回策略与 metadata 脱敏（去绝对路径）。

### Sprint 2（契约与工程化）

1. 统一提示词授权名与工具注册名（external_api -> fetch_external_data 或别名机制）。
2. 引入配置 schema 校验与启动期健康检查。
3. 梳理依赖分组并清理漂移依赖，统一启动文档与脚本命名。
4. 落地最小 CI 流水线（lint/type-check/test）。

### Sprint 3（能力升级）

1. 引入 reranker/混合检索。
2. 增加多场景 prompt 状态机（report/review/analysis）。
3. 接入链路观测平台并形成质量看板。

---

## 7. 结论

当前项目已具备“可运行的 Agent + RAG 骨架”，但在“安全边界、生命周期一致性、契约一致性”三个方面出现了新的工程化短板。下一阶段应优先完成安全治理与一致性修复，再推进评测、观测与能力增强，避免在功能扩展过程中放大技术债。

---

## 8. 最新改动（2026-03-18）

- Agent 规划增强：在 `agent/react_agent.py` 为复杂请求自动生成 ≤5 步的拆解规划，流式回显给用户并注入上下文，引导后续推理按步骤执行。
- 长期记忆能力：新增 `rag/memory_service.py` 基于现有 Chroma 向量库封装长期记忆服务；新增工具 `store_memory` / `search_memory`（由 `agent/tools/registry.py` 统一注册并在 ReactAgent 中加载），支持按 `user_id/scope/project` 写入与检索用户偏好、项目笔记等持久记忆。
- 工具集精简：移除与业务低相关的 `get_weather`/`get_user_location`/`get_user_id`/`get_current_month` 工具及关联变量，ReactAgent 工具列表同步精简，减少噪音与潜在安全面。

## 9. 最新改动（2026-03-20）

- 配置语义调整：
	- `model/config/agent.yml` 统一承载模型与推理配置（chat/embedding/base_url/api_key/temperature）
	- `agent/tools/config/rag.yml` 承载外部业务数据路径（`external_data_path`）
	- 相关模块引用已同步更新（`model/factory.py`、`agent/tools/modules/external_tools.py`、`rag/config_data.py`）
- Electron 连接设置增强：
	- 在前端新增 API 地址与 API 密钥输入面板
	- 支持本地持久化并自动回填（localStorage）
	- 支持运行时更新连接参数并重启托管后端后生效

## 10. 最新改动（2026-03-24）

- 文档与代码对齐：
	- 更新“当前架构 / 请求执行链路 / 本地开发流程”为 Gradio 主入口形态
	- 移除当前仓库不存在的 Electron 运行链路描述
- Middleware 模块说明补充：
	- 新增“2.5 Middleware 模块构成与作用”，覆盖工具调用监听、模型调用日志、动态提示词切换、trace 落盘机制
	- 补充工具事件队列机制（按 trace_id 维护 start/end/error），用于前端思考态显示“正在调用工具：xxx”

## 11. 最新改动（2026-04-08）

- 启动链路更新：
	- Windows 启动脚本统一为 `launch.bat`，并新增 PowerShell 版本 `Launch.ps1`，两者逻辑保持一致。
- 日志体系增强：
	- 新增 `LOG_LEVEL` 环境变量控制日志输出下限；
	- 增加 OpenAI 调用参数 debug 日志（模型名、温度、base_url、tools）。
- 依赖兼容修复：
	- 固定 `langgraph-prebuilt==1.0.8`，解决 `ExecutionInfo` 导入兼容问题。
- 聊天输出修复：
	- 修复流式输出中换行 token 丢失导致的 Markdown 结构粘连问题；
	- 优化聊天 Markdown 样式换行表现，提升复制与阅读一致性。
