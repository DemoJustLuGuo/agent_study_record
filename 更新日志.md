# 更新日志

说明：
- 使用语义化版本号记录每次改动。
- 版本格式：v主版本.次版本.修订号。
- 日期基于本地开发时间（当前记录为 2026-03-17）。

## v0.1.0 - 2026-03-17
### 变更摘要
- 完成项目源码分析，输出项目结构、执行链路、RAG流程与配置说明。
- 在 README 中生成首版项目总结文档。

### 涉及文件
- README.md

## v0.2.0 - 2026-03-17
### 变更摘要
- 将 README 重构为团队内开发版。
- 新增“当前架构、开发不足、可扩展内容、迭代节奏”等团队协作信息。

### 涉及文件
- README.md

## v0.3.0 - 2026-03-17
### 变更摘要
- 将前端入口从 Streamlit 迁移为轻量 Flask 方案。
- 新建 app 目录下的 Web 页面、静态资源与聊天接口。
- 引入 ReactAgent 惰性初始化，降低服务启动阶段耗时。

### 涉及文件
- app/main.py
- app/templates/index.html
- app/static/app.js
- app/static/styles.css
- README.md

## v0.4.0 - 2026-03-17
### 变更摘要
- 整合新增 RAG 知识库上传能力，避免与原项目产生双配置和双入口冲突。
- 统一新增模块到现有 config 配置体系。
- 在向量服务中新增文本直传入库能力（upload_text），复用同一去重逻辑与 Chroma 持久化。
- 新增 Flask 上传接口并在前端加入上传交互。

### 涉及文件
- rag/config_data.py
- rag/vector_store.py
- rag/knowledge_base.py
- rag/app_file_uploader.py
- app/main.py
- app/templates/index.html
- app/static/app.js
- app/static/styles.css
- README.md

## v0.5.0 - 2026-03-17
### 变更摘要
- 实现在线 RAG 问答链路：用户问题 -> 向量检索 -> 段落拼装提示词 -> 生成回答。
- 在 RAG 服务中新增结构化输出方法 answer_with_references，返回 answer 与 references。
- 新增在线接口 /api/rag/query。
- 前端新增“在线RAG直答”开关，支持展示命中片段摘要。
- 将 rag_summarize 提示词从占位文本升级为可执行模板。
- README 同步更新在线 RAG 架构与使用说明。

### 涉及文件
- rag/rag_service.py
- app/main.py
- app/templates/index.html
- app/static/app.js
- app/static/styles.css
- prompts/rag_summarize.txt
- README.md

## v0.5.1 - 2026-03-17
### 变更摘要
- 修正 README 中与当前文件结构不一致的描述，确保文档与现状一致。

### 涉及文件
- README.md

## v0.5.2 - 2026-03-17
### 变更摘要
- 生成并落地高灵活系统提示词模板，明确智能体核心角色为通信领域工程师。
- 在提示词中固化工具授权边界（web_search、python、external_api）。
- 强化经典 ReAct 执行约束，并规定最终输出必须为严格 JSON。
- 增加统一 JSON Schema、质量标准、风险校验与澄清机制，提升工程可复用性。

### 涉及文件
- prompts/main_prompt.txt

## v0.5.3 - 2026-03-17
### 变更摘要
- 补齐报告模式提示词，风格与主模式一致，且输出结构与 main_prompt 完全对齐（同一 JSON Schema）。
- 强化报告模式附加要求（摘要、正文组织、可执行建议），保证报告输出可直接交付。
- 完善 RAG 总结提示词，增加证据不足/证据冲突处理规则与结构化文本输出要求。

### 涉及文件
- prompts/report_prompt.txt
- prompts/rag_summarize.txt

## v0.5.4 - 2026-03-17
### 变更摘要
- 优化高优先级第2点：修复 fetch_external_data 的返回契约不一致问题。
- 将 fetch_external_data 统一为字符串返回（JSON 字符串），并在未命中时返回空字符串。
- 优化高优先级第3点：从 ReactAgent tools 列表移除内部函数 generate_external_data，避免模型误调用。
- README 技术债清单同步标注前3项高优先级问题已完成优化。

### 涉及文件
- agent/tools/agent_tools.py
- agent/tools/react_agent.py
- README.md

## v0.6.0 - 2026-03-17
### 变更摘要
- 优化中优先级第1点：get_current_month 改为基于系统时间动态返回月份。
- 优化中优先级第4点：listdir_with_allowed_type 在非法目录下改为返回空元组，修复返回值语义。
- 同步类型标注：vector_store 中文件列表类型与工具函数返回类型保持一致。
- README 技术债清单同步标注中优先级 4 项问题已完成优化（含已在先前版本完成的第2、3点）。

### 涉及文件
- agent/tools/agent_tools.py
- utils/file_handler.py
- rag/vector_store.py
- README.md

## v0.7.0 - 2026-03-17
### 变更摘要
- 新增 requirements.txt，补齐低优先级第1项“依赖锁定文件缺失”问题。
- 将 README.md 重命名为 开发规范.md，并同步规范文档标题与描述。
- 重新核查开发不足并更新规范：新增“缺少有效忽略规则文件”问题（.gitignore 缺失、.gltignore 为空）。
- 调整 Sprint 2 计划，补充忽略规则治理项。

### 涉及文件
- requirements.txt
- 开发规范.md
- 更新日志.md

## v0.8.0 - 2026-03-17
### 变更摘要
- 推进低优先级第3项：在向量服务中新增生命周期治理能力。
- 新增 source->md5 索引清单（chroma_manifest.json），补齐“数据源与索引状态”的可追踪性。
- 新增失效源文件同步清理能力（删除源文件后，可清理对应向量并同步更新 md5 指纹）。
- 新增快照与回滚能力（支持命令行与 API 两种方式），用于向量状态版本恢复。
- 推进低优先级第4项：新增 .gitignore，隔离日志、缓存、向量库持久化目录与本地产物。
- 开发规范同步更新：低优先级第3/4项标记为已完成，并补充生命周期管理说明。

### 涉及文件
- rag/vector_store.py
- rag/knowledge_base.py
- app/main.py
- config/chroma.yml
- .gitignore
- 开发规范.md
- 更新日志.md

## v0.9.0 - 2026-03-17
### 变更摘要
- 在 agent 工具层新增 python 工具，支持执行 Python 代码并返回 stdout/stderr，便于通信问题快速计算验证。
- 在 agent 工具层新增 matlab 工具，支持执行 MATLAB 代码；若本机未安装 MATLAB，会自动尝试 Octave 运行。
- 为 Python/MATLAB 工具补充超时控制、输出截断与异常回传，避免长时间阻塞或输出失控。
- 在 ReactAgent 工具列表中注册 python 与 matlab，使 Agent 在推理过程中可直接调用。
- 同步更新主模式与报告模式提示词授权，新增 matlab 能力声明与 JSON schema 字段枚举。

### 涉及文件
- agent/tools/agent_tools.py
- agent/tools/react_agent.py
- prompts/main_prompt.txt
- prompts/report_prompt.txt
- 更新日志.md

## v0.9.1 - 2026-03-17
### 变更摘要
- 在 agent 工具层新增联网搜索工具 web_search，支持基于公开网络信息进行快速核验。
- 在 ReactAgent 工具列表中注册 web_search，使模型可在推理中直接调用联网搜索能力。
- 明确当前执行优先级：Agent 模式下由模型先决策是否调用工具，不存在“固定先查知识库”的硬编码顺序；仅在模型调用 rag_summarize 时才进入知识库检索。

### 涉及文件
- agent/tools/agent_tools.py
- agent/tools/react_agent.py
- 更新日志.md

## v0.9.2 - 2026-03-17
### 变更摘要
- 调整 web_search 联网实现：不再使用 DuckDuckGo 接口，改为必应中国搜索入口（cn.bing.com）。
- 新增必应搜索结果 HTML 解析逻辑，提取标题、摘要与链接并按前5条返回。
- 保留超时、异常兜底与输出截断机制，确保工具稳定性。

### 涉及文件
- agent/tools/agent_tools.py
- 更新日志.md

## v0.9.3 - 2026-03-17
### 变更摘要
- 进一步将 web_search 实现升级为“无密钥接口模式”：改用必应中国 RSS 返回（cn.bing.com/search + format=rss）。
- 使用 XML 解析结果项（title/description/link），替代 HTML 结构抓取，降低页面结构变更导致失效的风险。
- 保持现有超时、异常处理与输出截断机制不变。

### 涉及文件
- agent/tools/agent_tools.py
- 更新日志.md


## v0.9.4 - 2026-03-17
### 变更摘要
- 根据交互展示需求，将主模式与报告模式提示词从“强制 JSON 输出”切换为“结构化纯文本输出”。
- 移除 JSON Schema 约束，改为统一文本段落模板，提升前端直读体验与流式可读性。
- 保留工具优先调用与工程质量约束，并明确工具失败时需要在文本中说明影响与替代方案。

### 涉及文件
- prompts/main_prompt.txt
- prompts/report_prompt.txt
- 更新日志.md

## v0.9.5 - 2026-03-17
### 变更摘要
- 在 `config/rag.yml` 新增 `SILICONFLOW_API_KEY` 变量，用于统一声明硅基流动 API 密钥的环境变量名。
- 调整模型工厂：`ChatSiliconFlow` 与 `SiliconFlowEmbeddings` 均通过该配置变量解析并注入 API 密钥。
- 所有模型调用入口统一指向 `rag.yml` 的 `SILICONFLOW_API_KEY` 配置，避免散落硬编码。

### 涉及文件
- config/rag.yml
- model/factory.py
- 更新日志.md

## v0.9.6 - 2026-03-17
### 变更摘要
- 新增启动期显式校验：当 `config/rag.yml` 已声明 `SILICONFLOW_API_KEY` 变量名但环境变量缺失时，启动阶段立即报错。
- 优化报错文案，明确提示缺失的环境变量名与处理方式，减少排障时间。

### 涉及文件
- model/factory.py
- 更新日志.md

## v0.9.7 - 2026-03-17
### 变更摘要
- 新增 Windows 一键启动脚本 `start_windows.bat`，支持双击启动项目。
- 启动脚本内置 Python 解释器探测、依赖检查/安装、`SILICONFLOW_API_KEY` 检查与交互输入。
- 支持从 `.env` 自动读取 `SILICONFLOW_API_KEY`（当当前环境变量未设置时），降低跨设备启动门槛。

### 涉及文件
- start_windows.bat
- 更新日志.md

## v0.9.8 - 2026-03-17
### 变更摘要
- 修复 Windows 一键启动脚本在未配置 `SILICONFLOW_API_KEY` 时触发的 CMD 解析错误（`此时不应有 :`）。
- 原因是 `set /p` 提示文本中的括号在代码块内未转义；现已改为无括号提示文案，脚本可正常进入启动流程。

### 涉及文件
- 一键启动.bat
- 更新日志.md

## v0.9.9 - 2026-03-17
### 变更摘要
- 审核 `requirements.txt` 依赖有效性，发现 `langchain-siliconflow==0.1.4` 在当前源不可安装，导致一键脚本自动安装阶段失败。
- 将依赖版本统一修正为与当前代码接口一致且可安装的 LangChain 1.x 版本组合。
- 复测一键脚本：依赖检查后可正常启动 Flask 服务。

### 涉及文件
- requirements.txt
- 更新日志.md

## v0.9.10 - 2026-03-17
### 变更摘要
- 修复一键启动脚本的导入路径问题：将启动命令从 `python app/main.py` 改为 `python -m app.main`。
- 启动前显式将项目根目录注入 `PYTHONPATH`，避免出现 `ModuleNotFoundError: No module named 'agent'`。

### 涉及文件
- 一键启动.bat
- 更新日志.md

## v0.10.0 - 2026-03-18
### 变更摘要
- 基于当前代码快照完成新一轮 LangChain Agent 项目审计，重新识别并分级新增技术债。
- 更新《开发规范》中的架构与工具说明，补充 web_search、python、matlab 等当前真实能力。
- 重写“当前开发不足”章节：新增安全边界、向量生命周期一致性、错误与路径暴露、契约漂移等问题。
- 同步刷新可扩展路线与迭代节奏，形成以“安全与一致性优先”的下一阶段执行计划。

### 涉及文件
- 开发规范.md
- 更新日志.md

## v0.11.0 - 2026-03-18
### 变更摘要
- 前端从 Flask 页面迁移为 Electron 桌面端（主进程 + preload + 渲染层）。
- 基于 Context7 检索 Electron 官方规范，采用 `contextIsolation + preload + contextBridge` 的安全 IPC 模式。
- 将原 Flask 页面能力迁移到 Electron：Agent 流式聊天、在线RAG直答、知识库 txt 上传、sync/snapshot/rollback 生命周期管理。
- Electron 主进程新增后端健康检查与自动拉起机制（优先复用已运行 Flask 服务，未运行时尝试启动 `python -m app.main`）。
- Flask 入口调整为 API-only 形态，根路由返回服务信息。
- 更新开发规范为“Electron 前端 + Flask API”架构，并补充 Electron 启动说明。

### 涉及文件
- electron/package.json
- electron/main.js
- electron/preload.js
- electron/renderer/index.html
- electron/renderer/app.js
- electron/renderer/styles.css
- electron/README.md
- app/main.py
- .gitignore
- 开发规范.md
- 更新日志.md

## v0.11.1 - 2026-03-18
### 变更摘要
- 更新 `一键启动.bat` 以适配 Electron 前端启动链路。
- 保留 Python 依赖检查与 `SILICONFLOW_API_KEY` 读取流程，保证 Electron 自动拉起后端时环境可用。
- 新增 Node.js/npm 可用性检查，以及 `electron/node_modules` 依赖校验与自动安装。
- 启动目标从 `python -m app.main` 调整为 `cd electron && npm start`。
- 修复 `PYTHON_EXE` 传递为相对路径可能导致 Electron 子进程找不到解释器的问题，改为优先写入绝对路径。

### 涉及文件
- 一键启动.bat
- 更新日志.md

## v0.11.2 - 2026-03-18
### 变更摘要
- 修复 `一键启动.bat` 在“检查依赖”阶段可能闪退的问题。
- 移除临时 Python 脚本依赖比对逻辑（该逻辑对本机 Python 环境兼容性较脆弱）。
- 新增 Python 版本硬校验（要求 3.10+）。
- 将依赖检查统一为稳定的 `pip install -r requirements.txt` 流程（具备幂等性，已安装依赖不会重复安装）。

### 涉及文件
- 一键启动.bat
- 更新日志.md



## v0.12.0 - 2026-03-19
### 变更摘要
- 后端观测性：重写中间件日志与 trace，支持模型/工具调用分段日志、JSONL trace 落盘；修复 `.result` 缺失导致的流式异常；恢复 UTF-8 控制台输出。
- 流式输出体验：隐藏策略/规划提示，不再注入模型上下文；为空内容增加 reasoning_content/tool_calls 兜底，解析内嵌 `<invoke>` 工具调用并直接执行，避免前端空白。
- 前端精简与 Markdown：Electron renderer 仅保留聊天页面，去除向量库/RAG 导入页；默认聊天消息按 Markdown 渲染。
- 启动脚本编码：一键启动脚本与 logger 强制 UTF-8，减少终端/前端乱码。

### 涉及文件
- agent/tools/middleware.py
- agent/tools/react_agent.py
- utils/log.py
- 一键启动.bat
- electron/renderer/index.html
- electron/renderer/app.js
- electron/renderer/styles.css

## v0.12.1 - 2026-04-08
### 变更摘要
- 补录 `9cdb089f` 之后的提交记录，确保版本追溯完整。
- 清理历史提交中的本地向量库二进制文件，减小仓库噪音与体积。
- 启动与日志能力升级：`一键启动.bat` 重命名为 `launch.bat`，新增 `Launch.ps1`，并补充 `LOG_LEVEL` 日志等级过滤。
- 修复依赖兼容问题：固定 `langgraph-prebuilt==1.0.8`，解决 `ExecutionInfo` 导入异常。
- 修复聊天输出问题：保留流式换行 token，避免 Markdown 在复制与渲染时出现结构粘连；优化样式换行表现。

### 涉及文件
- chroma_db/c9c13870-2d6b-4fd9-a00f-97c7af81e8b2/data_level0.bin
- chroma_db/c9c13870-2d6b-4fd9-a00f-97c7af81e8b2/header.bin
- chroma_db/c9c13870-2d6b-4fd9-a00f-97c7af81e8b2/length.bin
- chroma_db/c9c13870-2d6b-4fd9-a00f-97c7af81e8b2/link_lists.bin
- chroma_db/chroma.sqlite3
- launch.bat
- Launch.ps1
- README.md
- agent/middleware.py
- agent/react_agent.py
- app/main.py
- app/ui.css
- utils/log.py
- requirements.txt
- 更新日志.md

## v0.12.2 - 2026-04-09
### 变更摘要
- 启动链路优化：移除对 `.env` 的依赖，增强启动脚本的 Python 解释器探测与跨环境稳定性。
- 项目结构重构：将配置与提示词按模块归位，整理目录职责并减少跨模块路径耦合。
- RAG 能力增强：新增网页链接入库、升级为 Hybrid 检索 + 启发式 Rerank、重构分块策略，并增加在线运行指标面板。
- 展示与交互改进：修复 RAG Markdown 渲染与换行问题，优化在线 RAG 展示体验。
- UI 架构优化：将界面代码模块化拆分，`app/main.py` 精简为启动入口，提升可维护性。
- 智能体可观测与可解释性提升：强化工具失败反馈机制，统一输出“失败 + 原因 + 解决方案”，并明确备选策略行为。

### 涉及文件
- Launch.ps1
- launch.bat
- app/main.py
- app/app_builder.py
- app/runtime.py
- app/ui/chat_tab.py
- app/ui/rag_tab.py
- app/ui/knowledge_tab.py
- app/ui/connection_panel.py
- app/ui/__init__.py
- app/ui.css
- rag/vector_store.py
- rag/rag_service.py
- rag/metrics.py
- rag/config/chroma.yml
- rag/knowledge_base.py
- agent/react_agent.py
- agent/middleware.py
- agent/prompts/main_prompt.txt
- agent/prompts/report_prompt.txt
- agent/tools/modules/compute_tools.py
- agent/tools/modules/shared.py
- agent/tools/modules/web_tools.py
- agent/tools/modules/rag_tools.py
- agent/tools/modules/memory_tools.py
- agent/tools/modules/external_tools.py
- README.md
- 开发规范.md
- requirements.txt
- 更新日志.md

## v0.12.3 - 2026-04-09
### 变更摘要
- 移除与 `data/external/records.csv` 相关的功能。

### 涉及文件
- agent/tools/modules/external_tools.py
- agent/tools/registry.py
- agent/tools/agent_tools.py
- agent/tools/modules/__init__.py
- agent/tools/config/rag.yml
- data/external/records.csv
- utils/config_handler.py
- agent/prompts/main_prompt.txt
- agent/prompts/report_prompt.txt
- README.md
- 开发规范.md
- 更新日志.md

## v0.13.0 - 2026-04-23
### 变更摘要
- 智能体聊天升级为多会话模式：新增左侧垂直会话列表，支持会话新建、切换、重命名与关闭。
- 新增短期记忆持久化：接入 `langgraph-checkpoint-sqlite`，按会话线程将记忆保存到 `memory_db/thread_<n>.db`，并支持重启恢复历史会话内容。
- 增加会话压缩能力：在 Agent 中接入 `SummarizationMiddleware`，总结触发策略与总结模型参数统一外置到 `agent/config/memory_config.yml`。
- 增加会话命名策略：首次对话后自动生成一次会话标题，并支持用户手动重命名（长度限制 50 字）。
- 修复会话发送失败问题：修正 SQLite checkpointer 初始化方式，避免 `_GeneratorContextManager` 类型导致的智能体执行异常。
- 工程配套更新：新增记忆配置文件、更新依赖与忽略规则，并同步 README/开发规范文档。

### 涉及文件
- app/ui/chat_tab.py
- app/runtime.py
- app/chat_threads.py
- agent/react_agent.py
- agent/config/memory_config.yml
- utils/config_handler.py
- requirements.txt
- .gitignore
- README.md
- 开发规范.md
- 更新日志.md

## v0.13.1 - 2026-04-28
### 变更摘要
- 收敛 PDF 文档解析策略：移除 `marker` 与 `unstructured_pdf` PDF loader，只保留 `pypdf`。
- 同步更新默认 loader priority 与 `rag/config/chroma.yml`，避免 PDF 入库阶段触发外部 Marker 命令或 unstructured PDF 解析依赖。
- 新增 PDF loader 策略回归测试，锁定默认配置与运行时分发行为。
- 同步修正工具失败契约测试断言，匹配当前统一的“解决方案”返回格式。

### 涉及文件
- utils/file_handler.py
- rag/config/chroma.yml
- tests/test_file_handler_pdf_loader.py
- tests/test_tool_contracts.py
- 更新日志.md

## v0.13.2 - 2026-05-04
### 变更摘要
- 将 RAG 查询路径调整为只读行为，普通检索不再触发知识库自动同步。
- 为知识库生命周期写操作增加统一写锁，回滚快照必须提供与目标快照完全一致的确认名，并在回滚前创建预回滚快照。
- 拆分网页入库安全与快照文件操作到独立模块，为后续继续拆分 `rag/vector_store.py` 做边界准备。
- 新增服务层封装，Gradio 运行时开始通过 `services` 调用 RAG 查询、知识库管理和模型连接设置。
- 禁用 Gradio 知识库写操作的公开 `api_name`，避免 `show_api=True` 暴露同步、快照、回滚等管理能力。
- 增加 RAG 查询只读、回滚确认、网页入库安全、密钥保存安全和 Gradio API 暴露保护测试。

### 涉及文件
- app/runtime.py
- app/ui/knowledge_tab.py
- rag/vector_store.py
- rag/knowledge_base.py
- rag/rag_service.py
- rag/config/chroma.yml
- rag/ingest/web_loader.py
- rag/stores/snapshot_store.py
- services/settings_service.py
- services/rag_query_service.py
- services/knowledge_service.py
- tests/test_rag_readonly_query.py
- tests/test_knowledge_lifecycle_safety.py
- tests/test_web_ingest_security.py
- tests/test_gradio_knowledge_api_safety.py
- tests/test_settings_service_secrets.py
- 更新日志.md

## v0.14.0 - 2026-05-08
### 变更摘要
- 新增结构化 Agent 事件边界，保留 `execute_stream()` 旧字符串流兼容层，同时提供 `status`、`token`、`tool`、`done`、`error` 事件。
- 新增 `ChatService`，将会话线程、Agent 流式调用、自动命名和同线程并发保护从 Gradio 运行时中拆出。
- 将 `app/runtime.py` 调整为 Gradio 薄适配层，继续负责 Markdown/HTML 渲染和 `gr.update()` 输出。
- 新增 FastAPI 后端壳层，提供健康检查、聊天 SSE、RAG 查询、知识库管理、连接设置和 trace 查询路由。
- 为工具事件和 trace 查询增加脱敏投影，避免 API 暴露完整工具代码、完整参数或完整结果。
- 为 SQLite 会话连接增加 busy timeout，降低 FastAPI/Gradio 并发访问时的锁竞争风险。
- 增加 ChatService、Agent 事件、API 鉴权、SSE 与 trace 脱敏测试。

### 涉及文件
- agent/events.py
- agent/react_agent.py
- agent/middleware.py
- app/runtime.py
- app/chat_threads.py
- api/
- services/chat_service.py
- services/trace_service.py
- requirements.txt
- tests/test_agent_event_stream.py
- tests/test_chat_service.py
- tests/test_api_backend.py
- 更新日志.md

## v0.14.1 - 2026-05-15
### 变更摘要
- 为 FastAPI 后端补充 Pydantic 响应模型，固定 health、version、chat、RAG、knowledge、settings、traces 的第一版响应契约。
- 明确聊天 SSE 事件字段，约束 `status`、`token`、`tool`、`done`、`error` 等事件的前端消费形态。
- 新增 `docs/api.md`，记录后端启动方式、Admin Token、错误响应、SSE 事件、RAG/知识库/settings/trace API 契约。
- 按当前协作约定删除仓库已跟踪的 `tests/` 目录；本地一次性验证脚本继续受 `.gitignore` 的 `tests/` 规则忽略。

### 涉及文件
- api/schemas.py
- api/app.py
- api/routers/chat.py
- api/routers/rag.py
- api/routers/knowledge.py
- api/routers/settings.py
- api/routers/traces.py
- docs/api.md
- tests/
- 更新日志.md

## v0.14.2 - 2026-05-15
### 变更摘要
- 将 Gradio 默认定位收口为旧 UI / 本地调试入口，正式控制面继续由 FastAPI 提供。
- 将 Gradio `show_api` 改为默认关闭，仅在 `GRADIO_SHOW_API=1` 时显式打开调试 API 文档。
- 显式关闭聊天、RAG、连接设置和知识库管理等 Gradio UI 回调的公开 `api_name`，避免 Gradio API 继续暴露后端能力。
- 同步 README 与 API 文档，明确外部集成和管理操作应使用 FastAPI。

### 涉及文件
- app/main.py
- app/ui/chat_tab.py
- app/ui/connection_panel.py
- app/ui/knowledge_tab.py
- app/ui/rag_tab.py
- docs/api.md
- README.md
- 更新日志.md

## v0.15.0 - 2026-05-15
### 变更摘要
- 新增 `web/` React + TypeScript + Tailwind 前端控制台，覆盖 Chat、RAG、Knowledge、Settings、Traces 日常工作流。
- 前端通过 FastAPI 访问聊天 SSE、RAG 查询、知识库生命周期、连接设置和 trace 查询，Gradio 继续保留为旧 UI / 本地调试入口。
- 前端管理操作支持从 `VITE_ADMIN_TOKEN` 或 localStorage 读取本地 Admin Token，并统一附加 `Authorization: Bearer ...`。
- 收紧 FastAPI 公开响应模型，默认丢弃未声明的额外字段，降低服务层内部字段透传到前端的风险。
- 将后端 `/version` 同步到 `0.15.0`，并同步 README、API 文档和忽略规则。

### 涉及文件
- api/
- web/
- docs/api.md
- README.md
- .gitignore
- 更新日志.md

## v0.15.1 - 2026-05-15
### 变更摘要
- 移除前端 Settings 页面和 FastAPI settings 配置写入口，模型连接配置统一改为 `.env` 或系统环境变量管理。
- 后端启动时只读加载项目根目录 `.env`，不覆盖已存在的系统环境变量，也不通过 API 写入密钥或配置。
- 为 Knowledge 管理页面增加本地前置校验，阻止无 token、非法 URL、非法快照标签和未确认 rollback 请求。
- 新增 Playwright mock E2E，受控验证 Chat SSE 成功/错误流、Knowledge 安全边界和 Settings 导航移除。

### 涉及文件
- api/
- utils/env_loader.py
- web/
- .env.example
- docs/api.md
- README.md
- 更新日志.md

## v0.15.2 - 2026-05-15
### 变更摘要
- 移除旧的一键启动脚本 `Launch.ps1`、`launch.bat`，README 改为显式记录 FastAPI、React 前端和 Gradio 调试入口启动命令。
- 移除无确认清理脚本 `Remove-Logs.ps1`、`Remove-RagDB.ps1`，README 补充本地日志和 RAG 向量库的手动移除方式与注意事项。

### 涉及文件
- README.md
- Launch.ps1
- launch.bat
- Remove-Logs.ps1
- Remove-RagDB.ps1
- 更新日志.md

## 后续维护约定
- 每次提交或功能改造后，在本文件追加一个新版本段落。
- 建议一个功能里程碑对应一个次版本号递增（例如 v0.6.0、v0.7.0）。
- 若只做文案或小修复，使用修订号递增（例如 v0.5.2、v0.5.3）。
