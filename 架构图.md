## 项目架构示意（Mermaid）

```mermaid
flowchart LR
    subgraph Desktop客户端
        RDR[Electron Renderer\n聊天/UI/知识上传]
        PRE[Preload\ncontextBridge 暴露受限 API]
        RDR --> PRE --> MAIN[Electron Main\n窗口&IPC路由\n后端健康检查/自拉起]
    end

    subgraph 后端Flask
        MAIN -->|HTTP /api/chat| API["app/main.py\nFlask API"]
        MAIN -->|HTTP /api/rag/query| API
        MAIN -->|HTTP /api/knowledge/*| API
    end

    subgraph Agent层
        API --> RA[ReactAgent]
        RA --> MW[Middleware\nmonitor/log_before_model/report_switch]
        RA --> TOOLS[Tools]
    end

    subgraph 工具层主要功能
        TOOLS --> T1[rag_summarize\n基于知识库回答]
        TOOLS --> T2[web_search\nBing RSS 摘要]
        TOOLS --> T3[python/matlab\n代码执行]
        TOOLS --> T4[fetch_external_data\n业务外部数据]
        TOOLS --> T5[store_memory/search_memory\n长期记忆]
        TOOLS --> T6[fill_context_for_report\n切换报告模式]
    end

    subgraph RAG记忆
        T1 --> RAG["rags/rag_service.py\n检索+生成"]
        RAG --> VSVC["rag/vector_store.py\nChroma 向量库"]
        T5 --> MEM["rag/memory_service.py\n长期记忆写/查"]
        VSVC --> CH[(Chroma DB)]
        CH --> DATA[data/ 知识文件]
    end

    subgraph 配置与模型
        CFG[config/*.yml\n模型/向量库/提示词路径]
        PROMPTS[prompts/*.txt\n系统/报告提示词]
        MODEL["model/factory.py\nchat_model/embeddings"]
    end

    CFG -.-> RA
    CFG -.-> VSVC
    CFG -.-> MODEL
    PROMPTS -.-> RA
    MODEL --> RA
    MODEL --> VSVC
```

### 模块说明（简明中文）
- Electron Renderer/Preload/Main：桌面端 UI 与 IPC 桥接，负责把用户请求转发给本地后端并管理窗口、健康检查与自拉起。
- Flask API (`app/main.py`)：提供聊天、RAG 查询、知识库生命周期接口，对接桌面端。
- ReactAgent：统一封装 LangChain create_agent；接入中间件监控、动态提示词切换。
- 工具层：包含检索总结、联网搜索、代码执行、业务外部数据、长期记忆读写、报告模式上下文开关等。
- RAG/记忆：`rag_service` 负责检索+生成链；`vector_store` 管理 Chroma 知识库；`memory_service` 复用同库实现用户长期记忆；底层向量持久化在 `chroma_db/`，原始知识在 `data/`。
- 配置与模型：`config/*.yml` 管理模型、向量、提示词路径；`prompts/*.txt` 存储提示词；`model/factory.py` 暴露聊天/embedding 模型实例。
